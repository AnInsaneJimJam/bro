services:
  postgres:
    image: postgres:16-alpine
    environment: { POSTGRES_PASSWORD: postgres, POSTGRES_DB: bro }
    ports: ['5432:5432']
    volumes: ['bro-postgres:/var/lib/postgresql/data']
volumes: { bro-postgres: {} }
